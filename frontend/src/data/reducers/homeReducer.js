const initialState = {
  data: {}
}
export default function home (state = initialState, action) {
  switch (action.type) {
    // case ACTION_TYPE_ENUM:
    //     return {
    //         ...state,
    //         ...action,
    //     };
    default:
      return state
  }
}
